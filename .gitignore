.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
runs/
.env
data/anchors/*.pbf
dist/
.claude/

# API read caches — pure functions of a log, keyed on its (size, mtime).
# Deleting one costs a recompute, never correctness.
runs/**/*.stats.json
ui/node_modules/
ui/dist/
runs/managed/
